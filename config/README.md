# Shared Configuration

This directory contains configuration overlays shared by first-party ESP-IDF
projects. Project-specific settings should remain in each project's own
`sdkconfig.defaults` file.
